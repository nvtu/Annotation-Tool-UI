const initialState = {
    location: '',
    stress: '',
    activity: ''
}


export const annotationReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}